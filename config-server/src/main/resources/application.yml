server:
  port: 8888

spring:
  application:
    name: ead-config-server
  cloud:
    config:
      server:
        git:
          uri:  https://github.com/JoelMaciel/ead-config-server-repo
          default-label: main
          username: JoelMaciel
          password: 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

eureka:
  client:
    serviceUrl:
      defaultZone:  'http://localhost:8761/eureka'
  instance:
    hostname: localhost
